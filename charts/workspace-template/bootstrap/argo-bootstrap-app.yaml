apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: local-stack-bootstrap
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "file:///mnt/workspace/.git"
    targetRevision: main
    path: .
    helm:
      valueFiles:
      - values/localplane-addons.values.yaml
  destination:
    server: "https://kubernetes.default.svc"
    namespace: workspace
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
